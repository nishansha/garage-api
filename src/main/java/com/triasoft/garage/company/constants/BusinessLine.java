package com.triasoft.garage.company.constants;

/** What a Warehouse is allowed to transact — enforced server-side, not just UI display. */
public enum BusinessLine {
    VEHICLE_SALES,
    SERVICES
}
